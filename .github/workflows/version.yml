name: version-check-cicd

on:
  push:
    branches:
      - main


jobs:
  version-check:
    runs-on: ubuntu-latest
    steps:
    - name: print docker version
      run: docker version
    - name: python version check
      run: python --version
    - name: print node.js version
      run: node -v
    - name: Git version check
      run: git version
 

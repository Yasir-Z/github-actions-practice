name: secret-message

on:
  push:
    branches:
      - main


Jobs:
  secret-message:
    runs-on: ubuntu-latest
    steps:
      - name: Read and print secret
        run: echo "The secret is set + ":" + true"
